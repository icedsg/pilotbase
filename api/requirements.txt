# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# Database (Pilotbase internal)
sqlalchemy>=2.0.35
alembic>=1.13.3
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
sqlparse>=0.5.1          # splits multi-statement SQL scripts into individual statements

# Supported external DB drivers — SQL
pymysql>=1.1.1           # MySQL / MariaDB
aiosqlite>=0.20.0        # SQLite (async)
pymssql>=2.3.0           # MSSQL — needs the freetds-dev system package to build
oracledb>=2.4.0          # Oracle (pure-Python thin mode, no Instant Client needed)
ibm_db_sa>=0.4.0; platform_machine == "x86_64"  # DB2 (pulls in ibm_db, which fails to build on arm64)
sqlalchemy-cockroachdb>=2.0.2  # CockroachDB
snowflake-sqlalchemy>=1.6.1    # Snowflake
duckdb-engine>=0.13.0          # DuckDB (embedded analytical SQL, file or :memory:)

# Supported external DB drivers — NoSQL
pymongo>=4.8.0           # MongoDB
redis>=5.0.0             # Redis
cassandra-driver>=3.29.0 # Cassandra — needs the libev-dev system package to build its C extension
boto3>=1.34.0            # DynamoDB
# CouchDB has no dedicated driver here — CouchDBAdapter talks to its REST API
# directly over the httpx dependency already declared below.

# Supported external DB drivers — Vector
qdrant-client>=1.10.0    # Qdrant
chromadb>=0.5.0          # ChromaDB
weaviate-client>=3.26.0,<4.0.0  # Weaviate (v3 API)
pinecone>=5.0.0          # Pinecone
pymilvus>=2.4.0          # Milvus

# Settings & validation
pydantic>=2.9.0
pydantic-settings>=2.5.2
python-dotenv>=1.0.1

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=43.0.1

# AI agent
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.28

# HTTP client (for AI SDK calls)
httpx>=0.27.2

# WebSocket
websockets>=13.1

# Utilities
python-dateutil>=2.9.0
